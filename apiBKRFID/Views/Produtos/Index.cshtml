@model IEnumerable<apiBKRFID.Models.Produto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<p>
    @Html.ActionLink("Gerar Bloco K", "GerarBlocoK")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValueTag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodBarras)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValueTag)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodBarras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_Produto }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_Produto }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_Produto })
            </td>
        </tr>
    }

</table>
